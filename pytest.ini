[pytest]
minversion = 8.0
testpaths =
    apps/api/tests
    apps/worker/tests
    packages/extractors/tests
    packages/compliance/tests
    packages/exporters/tests
    packages/testkit/tests
asyncio_mode = auto
markers =
    needs_real_auth: do not stub get_current_user (real auth + DB integration)
