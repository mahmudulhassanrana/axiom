You are a principal engineer building a production-grade SaaS called "Axiom".

Axiom is a compliant, rate-limited, multi-format scraping and ingestion platform.

You must:

- Build a complete monorepo (apps + packages)
- Use FastAPI (backend), Next.js (frontend), PostgreSQL, Redis, Celery
- Follow clean architecture and modular design
- Enforce compliance (robots.txt, rate limits, no bypassing protections)
- Include Docker, tests, CI, documentation

IMPORTANT:

- Do NOT generate everything at once
- Build step-by-step in phases
- After each phase, wait for confirmation before continuing

Start with:

1. Repository structure
2. Base configs
3. Minimal running API
4. Minimal running web app

Return only code and files, no explanation.
