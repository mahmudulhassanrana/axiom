# Avoid npm arborist crash (npm 10.x): "Cannot read properties of null (reading 'matches')"
# when deduping workspace packages with file: links. See @npmcli/arborist Link.matches.
install-strategy=nested
legacy-peer-deps=true
