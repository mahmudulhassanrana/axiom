# axiom-testkit

Shared pytest fixtures (`pytest11` entry point `axiom_testkit`) and `sample_data` helpers for API, worker, and extractor tests.

Install as a dev dependency of each package; fixtures are available automatically once the distribution is installed.
